// é um bloco de comandos em que eu posso 
//executar em um momento oportuno

function nome(/*conjunto de prâmetros*/) {
    console.log("CBF Cursos")
}

nome(); // chamada da função

// dentro de um loop
for (let index = 0; index < 10 ; index++) {
    nome();
}

